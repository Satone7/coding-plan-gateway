export interface ThemeColors {
  brand: string;
  success: string;
  warning: string;
  error: string;
  muted: string;
}

export type ThemeName = 'tokyo-night' | 'catppuccin-mocha' | 'dracula' | 'nord' | 'custom';
