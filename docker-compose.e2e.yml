# Docker Compose for E2E Testing Environment
# Orchestrates the gateway and Claude Code test container

services:
  gateway:
    image: coding-plan-gateway:test
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: gateway
    ports:
      - "8080:8080"
    environment:
      - ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
      - LOG_LEVEL=debug
      - GATEWAY_URL=http://localhost:8080
      - AUTH_EXEMPT_PATHS=/health,/ready,/internal/*
    volumes:
      - ./e2e/test-config.yaml:/app/config.yaml:ro
      - ./logs/gateway:/app/logs
    networks:
      - e2e-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:8080/health"]
      interval: 5s
      timeout: 10s
      retries: 12
      start_period: 30s
    restart: unless-stopped

  claude-code:
    build:
      context: ./e2e
      dockerfile: Dockerfile
    container_name: claude-code
    environment:
      - ANTHROPIC_BASE_URL=http://gateway:8080
      - ANTHROPIC_MODEL=kimi-k2.5
      - ANTHROPIC_API_KEY=cpg_Qnhp8ZikLlKutijYis0R78n8HP0roxgk
    volumes:
      - ./logs/claude-code:/home/claude/.claude/logs
      - ./e2e/workspace:/workspace
    networks:
      - e2e-network
    depends_on:
      gateway:
        condition: service_healthy
    stdin_open: true
    tty: true
    restart: unless-stopped

networks:
  e2e-network:
    driver: bridge